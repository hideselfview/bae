---
alwaysApply: true
---
ALWAYS RUN GIT COMMANDS WITH `--no-pager`. For example `git diff`, which needs
to be run with `git --no-pager diff`, where `--no-pager` must be before `diff`.
This applies to all Git commands.